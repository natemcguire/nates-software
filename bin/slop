#!/usr/bin/env sh
TARGET="$0"
while [ -h "$TARGET" ]; do
  DIR="$(cd -P "$(dirname "$TARGET")" && pwd)"
  TARGET="$(readlink "$TARGET")"
  [ "${TARGET%${TARGET#?}}" = "/" ] || TARGET="$DIR/$TARGET"
done
DIR="$(cd -P "$(dirname "$TARGET")" && pwd)"
exec npx tsx "$DIR/slop.ts" "$@"

