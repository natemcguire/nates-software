// Buildspec string constants embedded for Cloudflare Pages Functions runtime
// (Pages Functions cannot read files from disk at runtime).
// Byte-identical to infra/codebuild/nsw-build-next.yml and infra/codebuild/nsw-deploy-next.yml.

export const NSW_BUILD_NEXT_BUILDSPEC = "version: 0.2\n# Next.js (OpenNext) candidate build. Runs UNTRUSTED user code (next.config,\n# npm lifecycle, next build). NO CF token here. Emits an S3 tarball of\n# .open-next/ + a PLATFORM-OWNED wrangler.jsonc. Env from control plane:\n# SOURCE_BUCKET, SOURCE_KEY, APP_ID, COMMIT_OID, ARTIFACT_BUCKET.\nenv:\n  variables:\n    OPENNEXT_VERSION: \"1.20.4\"\nphases:\n  install:\n    runtime-versions:\n      nodejs: 22\n  pre_build:\n    commands:\n      - mkdir -p /tmp/src\n      - aws s3 cp \"s3://${SOURCE_BUCKET}/${SOURCE_KEY}\" /tmp/source.tar\n      - tar -xf /tmp/source.tar -C /tmp/src\n      - cd /tmp/src\n      - npm ci || npm install\n      # Inject the adapter WITHOUT --legacy-peer-deps. An out-of-range user Next\n      # version fails HERE (ERESOLVE) — this is the intended fail-closed version gate.\n      - npm i -D --no-save @opennextjs/cloudflare@${OPENNEXT_VERSION}\n  build:\n    commands:\n      - cd /tmp/src\n      # Platform writes wrangler.jsonc for the dry-run size gate only;\n      # it is discarded and regenerated downstream in the trusted deploy stage.\n      - |\n        cat > wrangler.jsonc <<JSON\n        {\n          \"name\": \"nsw-app-${APP_ID}\",\n          \"main\": \".open-next/worker.js\",\n          \"compatibility_date\": \"2024-12-30\",\n          \"compatibility_flags\": [\"nodejs_compat\", \"global_fetch_strictly_public\"],\n          \"assets\": { \"directory\": \".open-next/assets\", \"binding\": \"ASSETS\" },\n          \"workers_dev\": true\n        }\n        JSON\n      # Platform owns open-next.config.ts; write unconditionally (overwrites any repo config).\n      - |\n        cat > open-next.config.ts <<TS\n        import { defineCloudflareConfig } from \"@opennextjs/cloudflare\";\n        export default defineCloudflareConfig({});\n        TS\n      - npx next build\n      - npx opennextjs-cloudflare build\n      # Optional tokenless script-size gate: surfaces script_too_large before deploy.\n      - npx wrangler deploy --dry-run --outdir /tmp/wdry 2>&1 | tee /tmp/dryrun.log\n      - tar -cf /tmp/opennext.tar .open-next wrangler.jsonc\n      - aws s3 cp /tmp/opennext.tar \"s3://${ARTIFACT_BUCKET}/${APP_ID}/${COMMIT_OID}/opennext.tar\"\n";

export const NSW_DEPLOY_NEXT_BUILDSPEC = "version: 0.2\n# Next.js OpenNext deploy runner. Holds CF API token (SSM). Consumes the inert\n# .open-next tarball (never runs user code). Env: APP_ID, COMMIT_OID,\n# CF_ACCOUNT_ID, ARTIFACT_BUCKET.\nenv:\n  variables:\n    WRANGLER_VERSION: \"4.127.1\"\n  parameter-store:\n    CLOUDFLARE_API_TOKEN: /nsw/cf-deploy-token\n  exported-variables:\n    - DEPLOYED_WORKER_URL\nphases:\n  install:\n    runtime-versions:\n      nodejs: 22\n    commands:\n      - npm install -g wrangler@${WRANGLER_VERSION}\n      - wrangler --version\n  pre_build:\n    commands:\n      - mkdir -p /tmp/app\n      - aws s3 cp \"s3://${ARTIFACT_BUCKET}/${APP_ID}/${COMMIT_OID}/opennext.tar\" /tmp/a.tar\n      - tar -xf /tmp/a.tar -C /tmp/app\n      - rm -f /tmp/app/wrangler.jsonc /tmp/app/wrangler.toml /tmp/app/wrangler.json\n      - |\n        cat > /tmp/app/wrangler.jsonc <<JSON\n        {\n          \"name\": \"nsw-app-${APP_ID}\",\n          \"main\": \".open-next/worker.js\",\n          \"compatibility_date\": \"2024-12-30\",\n          \"compatibility_flags\": [\"nodejs_compat\", \"global_fetch_strictly_public\"],\n          \"assets\": { \"directory\": \".open-next/assets\", \"binding\": \"ASSETS\" },\n          \"workers_dev\": true\n        }\n        JSON\n  build:\n    commands:\n      - cd /tmp/app\n      # Deploy stage needs ONLY wrangler — the .open-next/ dir is self-contained\n      # (proven: wrangler deploy from a node_modules-free dir succeeds).\n      - CLOUDFLARE_ACCOUNT_ID=${CF_ACCOUNT_ID} wrangler deploy 2>&1 | tee /tmp/deploy.log\n      - export WORKER_URL=$(grep -oE 'https://nsw-app-[a-z0-9-]+\\.[a-z0-9-]+\\.workers\\.dev' /tmp/deploy.log | head -1)\n      - |\n        if [ -z \"$WORKER_URL\" ]; then\n          echo \"NO_WORKER_URL: wrangler deploy produced no workers.dev URL\"\n          exit 1\n        fi\n      - export DEPLOYED_WORKER_URL=\"${WORKER_URL}\"\n      - echo \"DEPLOYED_WORKER_URL=${WORKER_URL}\"\n      - |\n        for i in $(seq 1 20); do\n          code=$(curl -s -o /dev/null -w \"%{http_code}\" --max-time 20 \"${WORKER_URL}/\" || echo 000)\n          echo \"smoke attempt $i: HTTP $code\"\n          if [ \"$code\" = \"200\" ]; then echo \"SMOKE_OK\"; SMOKE_OK=1; break; fi\n          sleep 5\n        done\n        if [ \"${SMOKE_OK:-0}\" != \"1\" ]; then echo \"SMOKE_FAILED: worker never returned 200\"; exit 1; fi\n      - 'echo \"{\\\"worker_url\\\":\\\"${WORKER_URL}\\\"}\" > /tmp/deploy-result.json'\nartifacts:\n  files: [ deploy-result.json ]\n  base-directory: /tmp\n";
